# Why is there two database.
- /database/ --> root level
- this is the schema creation blueprint. SQL script that allows us to create the database.
- used by docker


- scr/database
- python source code to interact with the database.
-- has thi files: connection.py and queries.py
- connection is used to connect to Post gre sql
- queries are used for Crud operations.


/database/init.sql     ← Schema (CREATE TABLE users...)
    ↓ Creates structure ↓
PostgreSQL Database
    ↑ Application queries ↑
/src/database/queries.py ← Python code (def get_user_ratings()...)